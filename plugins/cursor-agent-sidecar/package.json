{
  "name": "cursor-agent-sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw Cursor SDK provider sidecar.",
  "type": "module",
  "bin": {
    "cursor-agent-sidecar": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@cursor/sdk": "1.0.12"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
